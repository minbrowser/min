# 开发和打包说明

## 开发

CMD 进入项目目录执行：

```
npm run watch
```

另一个CMD 进入项目目录执行：

```
npm start
```

## 打包

```
npm run buildWindows
```

## 第五步：修改完成，GitGUI 提交 + 上传到自己 Github（对应 SVN 提交 + 上传）

### 5.1 本地提交代码

1. 重新打开 GitGUI（选中 D:\Code\MinBrowser 项目）
2. 左下角点Rescan（扫描改动文件），左边列出所有修改的文件
3. 选中所有变更文件 → Stage Changed（暂存，SVN 加入变更列表）
4. 下方 Commit Message 框写备注：feat:新增prompt弹窗+外部协议+设备唯一标识+设备密钥鉴权
5. 点Commit（本地提交，只存本地，没上传云端）

### 5.2 推送到你的 Github Fork 仓库
1. 菜单 Remote → Push（推送）
2. Remote 选origin，Branch 选你新建的feat-auth-prompt-protocol，点 Push
3. 弹出输入 Github 账号密码（HTTPS 地址），输入后完成上传
4. 打开 Github 你的 min 仓库，刷新就能看到上传的分支代码

## 第六步：后续同步官方 Min 新版本（定期操作）

官方更新代码后，拉原版代码合并到自己项目：

1. Remote → Fetch from → upstream（拉官方最新代码）
2. Branch → Checkout → main 切回主干
3. 合并 upstream/main 代码，解决冲突后再切回自己开发分支合并